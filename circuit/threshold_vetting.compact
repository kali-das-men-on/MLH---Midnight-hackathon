// ThresholdVetting.compact
// TODO(Person A): implement.
// Private input: balance
// Private input: threshold  (decided: private — neither balance nor the
//   required bar is revealed, only the pass/fail boolean)
// Output: boolean (balance >= threshold)
//
// witness balance: Uint64;
// witness threshold: Uint64;
// export circuit thresholdVetting(): Boolean {
//   return balance >= threshold;
// }
